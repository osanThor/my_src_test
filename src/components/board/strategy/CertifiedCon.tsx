import React from 'react';
import styled from 'styled-components';

const CertifiedCon = () => {
  return <CertifiedConBlock>인증 전략</CertifiedConBlock>;
};

const CertifiedConBlock = styled.div`
  width: 100%;
`;

export default CertifiedCon;
